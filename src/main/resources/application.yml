spring:
  application:
    name: netflix-eureka-naming-server
  devtools:
    add-properties: false
  cloud:
    config:
      enabled: false
server:
  port: 8761
  
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  instance:
    hostname: localhost